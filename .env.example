# ===========================================
# LyricLens Environment Configuration
# ===========================================
# Copy this file to .env.local and fill in your values

# -------------------------------------------
# Google Gemini API (Required)
# -------------------------------------------
# Get your API key from: https://aistudio.google.com/apikey
# Used for: Transcription, prompt generation, image generation (Imagen 3), and video generation (Veo)
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# -------------------------------------------
# DeAPI Video Animation (Optional)
# -------------------------------------------
# Alternative video provider that animates still images into video loops
# Get your API key from: https://deapi.ai
# Only required if using "deapi" as your video provider instead of Veo
VITE_DEAPI_API_KEY=your_deapi_api_key_here

# -------------------------------------------
# Notes
# -------------------------------------------
# - Environment variables prefixed with VITE_ are exposed to the frontend
# - The app will check for both VITE_ prefixed and non-prefixed versions
# - Video generation (Veo) requires a paid Gemini API plan
# - If Veo is not available, use DeAPI as an alternative or stick to image-only mode
